# Development Environment Configuration for Database Seeding

# Database Configuration - SQLite за local development
DB_TYPE=sqlite
DB_DATABASE=./data/dev.sqlite

# Seeding Configuration
SEEDER_ENVIRONMENT=dev
SEEDER_ENTITIES=
SEEDER_DESTRUCTIVE=true
SEEDER_VERBOSE=true

# Node Environment
NODE_ENV=development

# Docker Configuration (ще използва PostgreSQL когато е нужно)
COMPOSE_PROJECT_NAME=lms-seeder-dev