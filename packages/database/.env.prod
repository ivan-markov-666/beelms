# Production Environment Configuration for Database Seeding

# Database Configuration (should be overridden in production)
POSTGRES_DB=lms_prod
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production

# Seeding Configuration
SEEDER_ENVIRONMENT=prod
SEEDER_ENTITIES=
SEEDER_DESTRUCTIVE=false
SEEDER_VERBOSE=false

# Node Environment
NODE_ENV=production

# Docker Configuration
COMPOSE_PROJECT_NAME=lms-seeder-prod
