# beelms core – Sprint Status
project: "beelms-core"
# Коментар: Този файл следи реалното изпълнение на спринтовете и walking skeleton-ите.
# Планът (какво ИСКАМЕ да направим) е в beelms-core-sprint-planning.md.

sprint_length_weeks: 2
current_sprint: null  # Попълни при старт на първия реален спринт, напр. "S1".

sprints:
  - id: "S1"
    name: "Sprint 1 – WS-CORE-1 (Core Wiki Vertical)"
    status: "done"  # allowed: planned | in_progress | done
    walking_skeletons:
      - "WS-CORE-1"
    goals:
      - "Core Wiki vertical: списък и детайл на статия за Guest (FE+API+DB)"

  - id: "S2"
    name: "Sprint 2 – WS-CORE-2 (Auth + Wiki integration)"
    status: "done"
    walking_skeletons:
      - "WS-CORE-2"
    goals:
      - "Register/Login vertical + свързване на логнат потребител с Wiki"

  - id: "S3"
    name: "Sprint 3 – WS-CORE-3 (Admin Dashboard) + WS-CORE-4 prep"
    status: "done"
    walking_skeletons:
      - "WS-CORE-3"
    goals:
      - "Admin dashboard с базови метрики (users/articles)"
      - "Подготовка за CLI `npx create-beelms-app` (WS-CORE-4)"

walking_skeletons:
  - id: "WS-CORE-1"
    name: "Guest → Wiki List → Wiki Article"
    status: "done"  # planned | in_progress | done
    notes: "Реализиран FE+API+DB vertical (/wiki + /wiki/[slug] + /api/wiki/articles + /api/wiki/articles/:slug) според текущия код; описан е в docs/sprint-artifacts/beelms-core-sprint-planning.md."

  - id: "WS-CORE-2"
    name: "Guest → Register/Login → View Wiki"
    status: "done"
    notes: "Реализиран Auth + Wiki vertical (/auth/register + /auth/login + header login state, /api/auth/register + /api/auth/login + /api/users/me, redirect към /wiki) според текущия код; описан е в beelms-core-sprint-planning.md."

  - id: "WS-CORE-3"
    name: "Admin Dashboard & Basic Settings"
    status: "done"
    notes: "Реализиран Admin vertical (/admin dashboard с метрики от /admin/metrics/overview, admin навигация към /admin/wiki и /admin/users, BE AdminMetricsService/AdminMetricsController/AdminUsersController с реални броячи за потребители и статии) според текущия код; описан е в beelms-core-sprint-planning.md. Подготовката за WS-CORE-4 (CLI) остава бъдеща работа."

  - id: "WS-CORE-4"
    name: "DX & CLI – npx create-beelms-app"
    status: "done"
    notes: "WS-CORE-4 е реализиран като локален TypeScript CLI прототип в monorepo-то (tools/create-beelms-app), който scaffold-ва нов проект (api/, по избор web/, env/, docker/), генерира docker-compose.yml с project-specific имена/портове, добавя DX helper скриптове за regression тестове и README. Има smoke тест (npm run smoke), който валидира walking skeleton-а CLI → Docker → npm run test:regression:local за нов проект, и е добавен локален \"бедняшки CI\" ритуал + примерни pre-push hook-ове и коренен CHANGELOG.md с v0.1.0 DX/CLI baseline. Оставаща бъдеща работа: публикуване като реална npx create-beelms-app команда извън това репо, по-богати DX флагове/интерактивен UX и CI шаблони."

notes:
  - "Статусите тук са за изпълнението по beelms core. Legacy QA4Free може вече да има част от тези потоци, но не са систематично мапнати тук."
  - "При старт на реален sprint, актуализирай current_sprint и съответните status полета на 'in_progress'/'done'."
