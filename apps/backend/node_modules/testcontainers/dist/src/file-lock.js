"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.withFileLock = void 0;
const path_1 = __importDefault(require("path"));
const tmp_1 = __importDefault(require("tmp"));
const promises_1 = require("fs/promises");
const logger_1 = require("./logger");
const proper_lockfile_1 = __importDefault(require("proper-lockfile"));
function withFileLock(fileName, fn) {
    return __awaiter(this, void 0, void 0, function* () {
        const file = yield createEmptyTmpFile(fileName);
        let releaseLockFn;
        try {
            logger_1.log.debug(`Acquiring lock file "${file}"...`);
            releaseLockFn = yield proper_lockfile_1.default.lock(file, { retries: { forever: true } });
            logger_1.log.debug(`Acquired lock file "${file}"`);
            return yield fn();
        }
        finally {
            if (releaseLockFn) {
                logger_1.log.debug(`Releasing lock file "${file}"...`);
                yield releaseLockFn();
                logger_1.log.debug(`Released lock file "${file}"`);
            }
        }
    });
}
exports.withFileLock = withFileLock;
function createEmptyTmpFile(fileName) {
    return __awaiter(this, void 0, void 0, function* () {
        const file = path_1.default.resolve(tmp_1.default.tmpdir, fileName);
        yield (0, promises_1.writeFile)(file, "");
        return file;
    });
}
