import { PartialDockerClient } from "./docker/client/docker-client-types";
import Dockerode from "dockerode";
export declare function registerSessionIdForCleanup(sessionId: string): Promise<void>;
export declare function registerComposeProjectForCleanup(project: string): Promise<void>;
export declare function startReaper(dockerClient: PartialDockerClient, sessionId: string, existingReaperContainer?: Dockerode.ContainerInfo): Promise<void>;
