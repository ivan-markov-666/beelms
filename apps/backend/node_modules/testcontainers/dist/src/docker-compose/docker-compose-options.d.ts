import { Environment } from "../docker/types";
import { Logger } from "../logger";
import { IDockerComposeOptions } from "docker-compose";
export type DockerComposeOptions = {
    filePath: string;
    files: string | string[];
    projectName: string;
    commandOptions?: string[];
    composeOptions?: string[];
    environment?: Environment;
    logger?: Logger;
};
export declare const defaultDockerComposeOptions: ({ environment, ...options }: DockerComposeOptions) => Promise<Partial<IDockerComposeOptions>>;
export type DockerComposeDownOptions = {
    timeout: number;
    removeVolumes: boolean;
};
