import { DockerInfo } from "./docker/functions/get-info";
import Dockerode from "dockerode";
export type SystemInfo = {
    nodeInfo: NodeInfo;
    dockerInfo: DockerInfo;
    dockerComposeInfo?: DockerComposeInfo;
};
export declare const getSystemInfo: (dockerode: Dockerode) => Promise<SystemInfo>;
type NodeInfo = {
    version: string;
    architecture: string;
    platform: string;
};
export type DockerComposeInfo = {
    version: string;
    compatability: DockerComposeCompatibility;
};
export type DockerComposeCompatibility = "v1" | "v2";
export {};
