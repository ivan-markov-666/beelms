"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSystemInfo = void 0;
const logger_1 = require("./logger");
const docker_compose_1 = __importStar(require("docker-compose"));
const get_info_1 = require("./docker/functions/get-info");
let systemInfo;
const getSystemInfo = (dockerode) => __awaiter(void 0, void 0, void 0, function* () {
    var _a, _b;
    if (systemInfo !== undefined) {
        return systemInfo;
    }
    logger_1.log.debug("Fetching system info...");
    const nodeInfo = getNodeInfo();
    const dockerInfo = yield (0, get_info_1.getDockerInfo)(dockerode);
    const dockerComposeInfo = yield getDockerComposeInfo();
    systemInfo = { nodeInfo, dockerInfo, dockerComposeInfo };
    logger_1.log.debug(`Node version: ${nodeInfo.version}, Platform: ${nodeInfo.platform}, Arch: ${nodeInfo.architecture}, OS: ${dockerInfo.operatingSystem}, Version: ${dockerInfo.serverVersion}, Arch: ${dockerInfo.architecture}, CPUs: ${dockerInfo.cpus}, Memory: ${dockerInfo.memory}, Compose installed: ${dockerComposeInfo !== undefined}, Compose version: ${(_a = dockerComposeInfo === null || dockerComposeInfo === void 0 ? void 0 : dockerComposeInfo.version) !== null && _a !== void 0 ? _a : "N/A"}, Compose compatibility: ${(_b = dockerComposeInfo === null || dockerComposeInfo === void 0 ? void 0 : dockerComposeInfo.compatability) !== null && _b !== void 0 ? _b : "N/A"}`);
    return systemInfo;
});
exports.getSystemInfo = getSystemInfo;
const getNodeInfo = () => {
    return {
        version: process.version,
        architecture: process.arch,
        platform: process.platform,
    };
};
const getDockerComposeInfo = () => __awaiter(void 0, void 0, void 0, function* () {
    try {
        return {
            version: (yield docker_compose_1.default.version()).data.version,
            compatability: "v1",
        };
    }
    catch (err) {
        try {
            return {
                version: (yield docker_compose_1.v2.version()).data.version,
                compatability: "v2",
            };
        }
        catch (_c) {
            logger_1.log.warn(`Unable to detect DockerCompose version, is it installed? ${err}`);
            return undefined;
        }
    }
});
