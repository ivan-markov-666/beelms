#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../sha.js@2.4.12/node_modules/sha.js/bin.js" "$@"
else
  exec node  "$basedir/../../../../../sha.js@2.4.12/node_modules/sha.js/bin.js" "$@"
fi
